# Trae Agent — Knowledge Base

## Conventions
- Always read `CLAUDE.md` as the primary workspace guideline, not `AGENTS.md`.
- Before any change, sync with upstream: `git fetch upstream && git rebase upstream/main` (or `git pull upstream main`).
