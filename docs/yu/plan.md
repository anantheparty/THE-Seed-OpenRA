## Current
Execute Phase 0.1b by migrating required NLU rule assets into `nlu_pipeline/rules/`, updating imports off `the_seed`, and removing the `the-seed` submodule and `.gitmodules` references.

## Queue
1. Return `plan.md` to `Idle.` after Phase 0 completion and append final progress entries with verification details.
